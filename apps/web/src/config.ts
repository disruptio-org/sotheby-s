/// <reference types="vite/client" />

/** How long a toast stays on screen, in ms. */
export const TOAST_DURATION = 4200;

/** Characters of step output shown before the panel offers to expand. */
export const OUTPUT_PREVIEW_CHARS = 600;
