# Skip the sign-in screen and boot straight into the back office.
VITE_START_LOGGED_IN=false

# Milliseconds each workflow step stays in the "A executar" state (300-2500).
VITE_WORKFLOW_STEP_DELAY=1000
